// prompt for age and output to the console
const myAge = prompt("How old are you?");
console.log(myAge);

// prompt for a different kind of value
const isTruth = confirm("Click OK if what you just entered is true, Cancel if not");
console.log(isTruth);
